[package]
name = "decryptor"
version = "0.1.0"
edition = "2024"

[profile.release]
opt-level = 2
lto = "fat"
codegen-units = 1
debug = false
strip = "symbols"
panic = "abort"

[dependencies]
lib = { path = "../shared" }